An implementation of the malloc library using a segregated linked list to organize data.
Contains a malloc, realloc, calloc and free function with a heap checker that shows the structure and contents of the heap
